{
  "name": "react-quiz-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "server": "json-server --watch db.json -p 8801",
    "server: help": "json-server --help"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
